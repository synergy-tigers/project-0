const menuToggle = document.getElementById("menuToggle");

// when the menutoggle is focused, add an event listener

function keyPress(event) {
  // define keycode to be used
  // when the keycode is hit, then activate the checkbox and menu
}

// grab the api url

// use a try catch block here
// connect to the api

// grab data from the api

// display data from the api in this div id